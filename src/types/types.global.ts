import { ReactNode } from "react";

export type TContainerProps = {
  children: ReactNode;
};

export type TBtnProps = {
  btnText: string;
};

export type TLinkProps = {
  linkUrl: string;
  linkText: string;
};
